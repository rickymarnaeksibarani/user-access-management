spring:
  profiles:
    active: local
  application:
    name: user_management
  output:
    ansi:
      enabled: ALWAYS
  cloud:
    config:
      fail-fast: true
      uri: http://172.16.41.107:8090
      enabled: true
  devtools:
    add-properties:
      false